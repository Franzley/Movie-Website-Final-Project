import React, { Component } from "react";

const placeholder = {
  background: "grey",
  position: "absolute",
  bottom: "0",
  width: "100%"
};


export const Footer = () => (
  <footer style={placeholder}>Footer</footer>
);
